specVersion: "v1"
serviceId: "cafee"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cafee-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
